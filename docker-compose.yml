nginx:
  image: nginx:1.9.5
  ports:
    - "80:80"
  links:
    - php55
    - memcached
  extra_hosts:
    - "local.appsymfony.com:127.0.0.1"
  volumes:
    - ./app:/usr/share/nginx/html
    - ./nginx/conf.d/app.conf:/etc/nginx/conf.d/default.conf
    - ./nginx/logs:/var/log/nginx
memcached:
  image:  memcached
php55:
  image: php:5.5-fpm
  ports:
    - "9000:9000"
  volumes:
    - ./app:/var/www/html

#  nginx
#  docker run -p 80:80 -v `pwd`/app/:/usr/share/nginx/html/app -v `pwd`/nginx/conf.d/app.conf:/etc/nginx/conf.d/default.conf --link phpapp2:testesymfony nginx

#  using image phpmentors
# docker run --name teste2 -v `pwd`/app:/var/app/web -p 8080:80 -d phpmentors/symfony-app:php55
